import React from 'react';
import ReactDOM from 'react-dom/client';
import WeatherApp from './view/WeatherApp/WeatherApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
   <WeatherApp/>
  </React.StrictMode>
)